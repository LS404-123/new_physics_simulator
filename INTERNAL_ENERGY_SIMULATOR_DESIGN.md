# 內能互動模擬器設計

## 教學目標

- 對象：正在學習內能粒子模型的 HKDSE 學生。
- 核心困難：學生容易忽略粒子數量對物體總內能的影響。
- 核心理解：物體的內能是所有粒子的總動能（KE）與總勢能（PE）之和。
- 學習成果：學生能分辨平均動能與總動能，並解釋質量、溫度及物態如何影響內能。

## 學習流程

### 1. 預測

固定比較兩杯同為 25°C 的水：A 杯為 100 mL，B 杯為 300 mL。學生先回答：

1. 哪杯水的內能較大？
2. 哪杯水所有粒子的總 KE 較大？

題目明確限定初始狀態，避免學生在自由調校溫度後誤以為答案仍然固定。

### 2. 驗證

提交預測後顯示三條共用比例尺的能量條：總 KE、總 PE、內能 U。能量條只比較大小，不顯示數值。

### 3. 自由模擬

解鎖兩個物體的所有控制：

- 質量：100–500 g；
- 冰的溫度：-20–0°C；
- 水的溫度：0–100°C；
- 物態：冰／水。

切換物態時，若目前溫度超出新物態的範圍，溫度自動調整至 0°C。學生可自由組合條件，比較兩個物體的能量條。

## 物理模型

- 粒子數與質量成正比；畫面粒子只作數量示意，不逐粒計算。
- 總 KE 隨粒子數及絕對溫度增加。
- 同一物態內，升溫主要增加 KE。
- 相同質量、相同溫度下，水的 PE 大於冰，以表示熔化增加的粒子勢能。
- 所有動畫、粒子示意及能量條均由同一模擬狀態產生。
- U 的能量條必須滿足 U = KE + PE。

## 介面

- 固定 1140×768 CSS px 的 iPad Air 橫向畫面，不容許水平或垂直捲動。
- 左側：兩個透明容器與粒子示意。
- 右側：預測／解釋、兩組控制器、KE／PE／U 能量條。
- 控制器具備至少 44 px 觸控目標、鍵盤操作、可見焦點狀態及 reduced-motion 支援。
- 提供重設按鈕，返回固定的初始預測狀態。

## 技術方案

- 建立一個根目錄，入口為 `index.html`。
- 使用原生 HTML、CSS 及 JavaScript，不加入框架或新依賴。
- 將物理計算保持為可獨立測試的最小函數。
- 建立 `preview.png`，並把新項目加入根目錄 `simulations.js` 的 `simulations` 陣列末端。

## 驗證

- 最小物理檢查：增加質量會增加總能量；同質量同溫下水的 PE 大於冰；U = KE + PE；切換物態會修正超出範圍的溫度。
- 檢查 HTML／JavaScript 語法。
- 使用 In-App Browser 驗證預測、揭示、所有滑桿、物態切換、重設及鍵盤操作。
- 驗證 1140×768 畫面沒有捲動或裁切。
- 驗證主頁的最新模擬器、目錄卡、搜尋、課題篩選、預覽圖及連結。

## 非功能假設

- 純本機單頁運作，不收集或傳送個人資料。
- 同一時間只供一名學生在一部裝置使用，沒有後端、帳戶或多人同步。
- 不要求離線安裝、資料保存或分析紀錄。
- 維護方式沿用現有專案的原生 JavaScript 模式。

## 明確不包括

- 連續熔化或沸騰動畫；
- 熱傳遞速率；
- 能量數值或絕對能量刻度；
- 粒子速率分布；
- 逐粒子物理計算。

## 決策紀錄

1. 採用「固定預測 → 揭示證據 → 自由模擬」流程；比一開始完全自由更能建立清楚的因果比較。
2. 自由模式保留兩個物體的全部滑桿及冰／水切換，以便學生自行建立不同情境。
3. 使用質量而非體積作控制量，避免冰與水密度不同造成額外混淆。
4. 能量條不顯示數值或「相對能量」字樣，只呈現共同尺度下的大小。
5. 冰與水使用各自合理的溫度範圍，切換物態時自動修正溫度。
6. 不加入相變過程或額外框架，保持教學焦點及最小實作範圍。
